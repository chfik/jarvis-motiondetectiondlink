*ALLUM*DETECTION*|*LANC*DETECTION*|*DEMARR*DETECTION*==say "La détection de mouvement sera active d'ici 15 secondes. Bonne journée." && sleep 15 && "${motiondetectiondlink_cmdenable}"
*ETEIN*DETECTION*|*ARRET*DETECTION*|*STOP*DETECTION*==say "La détection de mouvement est arrêtée" && "${motiondetectiondlink_cmddisable}"
